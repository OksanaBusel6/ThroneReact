import React from 'react';
import styled from 'styled-components';

const HomeTitle = styled.h1`
  font-size: 50px;
  text-align: center;
  color: #fff;
`;

const HomePage = () => {
  return (
    <HomeTitle>Wolcom to GOT DB</HomeTitle>
  )
};

export default HomePage;